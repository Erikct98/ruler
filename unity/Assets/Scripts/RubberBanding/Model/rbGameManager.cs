using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class rbGameManager : MonoBehaviour
{
    // point set:
    // define...

    // removed point set (maybe?):
    // define...

    // current convex hull:

    // Start is called before the first frame update
    void Start()
    {
        // @Jurrien van Winden
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    // Initializes the game's initial state and playing field
    void InitGame() 
    {
        // @Jurrien van Winden
    }

    // Start the game so that the players can play
    void startGame()
    {
        // @Jurrien van Winden
    }

    // Method to remove the chosen point, recompute the convex hull, assign scores, and pass the turn to the other player
    void NextTurn()
    {
        // @Jurrien van Winden
    }

    // Removes a point from the level
    void RemovePoint()
    {
        // @Jurrien van Winden
    }
}